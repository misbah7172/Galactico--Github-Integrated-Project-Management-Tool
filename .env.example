# ===========================================
# GITHUB OAUTH CONFIGURATION
# ===========================================

# GitHub OAuth Application (Web App)
GITHUB_CLIENT_ID=your_web_app_client_id_here
GITHUB_CLIENT_SECRET=your_web_app_client_secret_here

# ===========================================
# APPLICATION URLS
# ===========================================

# Base application URL (using Cloudflare tunnel)
# Note: The actual URL will be dynamically generated by Cloudflare
# You'll see it in the Docker logs when the container starts
BASE_URL=https://your-tunnel-url.trycloudflare.com
OAUTH_REDIRECT_URI=https://your-tunnel-url.trycloudflare.com/login/oauth2/code/github

# ===========================================
# CLOUDFLARE TUNNEL CONFIGURATION
# ===========================================

# Cloudflare tunnel token (pre-configured)
TUNNEL_TOKEN=34EiSXp1nxwkIhmbPIOi4JZr3ay_3ZGiXzsZc56w3J81vwzD1

# ===========================================
# SECURITY CONFIGURATION
# ===========================================

# JWT Secret (minimum 32 characters)
JWT_SECRET=your_jwt_secret_key_here_minimum_32_characters_required

# Encryption Key (exactly 32 characters)
ENCRYPTION_KEY=your_encryption_key_32_characters_exactly

# ===========================================
# EMAIL CONFIGURATION (Optional)
# ===========================================

# Email settings for notifications
SPRING_MAIL_HOST=smtp.gmail.com
SPRING_MAIL_PORT=587
SPRING_MAIL_USERNAME=your_email@gmail.com
SPRING_MAIL_PASSWORD=your_app_password

# ===========================================
# DATABASE CONFIGURATION
# ===========================================
# Database is already configured to use NeonDB
# No additional configuration needed

# ===========================================
# DEPLOYMENT INFORMATION
# ===========================================
# The application will automatically:
# 1. Connect to the online PostgreSQL database (NeonDB)
# 2. Start Cloudflare tunnel with your authentication token
# 3. Generate a dynamic HTTPS URL (visible in Docker logs)
#
# To run: docker-compose up --build
#
# IMPORTANT: After startup, check the Docker logs to see your
# actual Cloudflare tunnel URL, which will look like:
# https://random-words-12345.trycloudflare.com

# OAuth redirect URLs
OAUTH_REDIRECT_URI=https://your-domain.loca.lt/login/oauth2/code/github
OAUTH_EXTENSION_REDIRECT_URI=https://your-domain.loca.lt/api/auth/oauth-callback

# VS Code Extension callback URL
VSCODE_CALLBACK_URL=vscode://vscode.github-authentication/did-authenticate

# ===========================================
# DATABASE CONFIGURATION
# ===========================================

# PostgreSQL Database (NeonDB)
DB_HOST=ep-royal-dew-ad5ccvfm-pooler.c-2.us-east-1.aws.neon.tech
DB_PORT=5432
DB_NAME=neondb
DB_USERNAME=neondb_owner
DB_PASSWORD=npg_OimpNMHWXR39
DB_URL=jdbc:postgresql://ep-royal-dew-ad5ccvfm-pooler.c-2.us-east-1.aws.neon.tech:5432/neondb?sslmode=require

# ===========================================
# EMAIL CONFIGURATION
# ===========================================

# Gmail SMTP Configuration
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=your_email@gmail.com
MAIL_PASSWORD=your_gmail_app_password_here

# ===========================================
# EXTERNAL SERVICES
# ===========================================

# Slack Integration
SLACK_WEBHOOK_URL=your_slack_webhook_url_here

# GitHub API
GITHUB_API_BASE_URL=https://api.github.com

# ===========================================
# DEVELOPMENT SETTINGS
# ===========================================

# Server Configuration
SERVER_PORT=5000
SERVER_ADDRESS=0.0.0.0

# Environment
ENVIRONMENT=development
DEBUG_MODE=true

# Session timeout (in minutes)
SESSION_TIMEOUT=30

# ===========================================
# SECURITY SETTINGS
# ===========================================

# JWT Secret (for token signing) - Generate a strong random key
JWT_SECRET=your-super-secret-jwt-key-here-change-this-in-production

# Encryption key for sensitive data - Generate a strong random key
ENCRYPTION_KEY=your-encryption-key-here-change-this-in-production

# ===========================================
# EXTENSION SPECIFIC SETTINGS
# ===========================================

# Extension authentication URLs
EXTENSION_AUTH_URL=https://your-domain.loca.lt/api/auth/login-url
EXTENSION_TOKEN_DISPLAY_URL=https://your-domain.loca.lt/api/auth/token-display
EXTENSION_CALLBACK_URL=https://your-domain.loca.lt/api/auth/callback

# ===========================================
# ADDITIONAL CONFIGURATION
# ===========================================

# File upload limits (in MB)
MAX_FILE_SIZE=10
MAX_REQUEST_SIZE=10

# Logging levels
LOG_LEVEL_ROOT=INFO
LOG_LEVEL_AUTOTRACK=DEBUG

# ===========================================
# SETUP INSTRUCTIONS
# ===========================================
# 1. Copy this file: cp .env.example .env
# 2. Replace all placeholder values with your actual credentials
# 3. Never commit the .env file to version control
# 4. Keep your credentials secure and rotate them regularly
